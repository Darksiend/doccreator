let spaceSection = {
  properties: { type: SectionType.CONTINUOUS },
  children: [mainTable],
};

const {
  Paragraph,
  SectionType,
  Table,
  WidthType,
  TableRow,
  AlignmentType,
  TableCell,
  Header,
  VerticalAlign,
  PageNumber,
} = docx;
import docx from "docx";
